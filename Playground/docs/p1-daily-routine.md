# P1 Daily Routine (09:00)

1. Open `http://localhost:3110/command`
2. Confirm 5-field briefing (mail, approvals, projects, CFO, sangfor)
3. Review pending approvals at `/approvals`
4. Click mail/finance/sangfor widgets — same-tab navigation
5. Run `node scripts/c-stack-health.mjs` if any widget shows unhealthy
